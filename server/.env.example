# Copie para .env (gitignored) e preencha.
# Chave da API da Anthropic — NUNCA commitar a real.
ANTHROPIC_API_KEY=

# PAT do GitHub (fine-grained, Contents: Read and write) — usado só em projetos tipo "codigo".
GITHUB_TOKEN=

# Porta da ponte local.
PORTA=3777
